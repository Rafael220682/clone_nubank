import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        flexDirection: "row",
        padding: 8,

        backgroundColor:"#e6e6e6",
        borderRadius: 8,
    },

    textCard:{
        marginLeft: 8,
        fontWeight: "bold",
        fontSize: 20,
    }

    
})